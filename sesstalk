#!/usr/bin/env bash
set -euo pipefail
PY="${SESSTALK_PYTHON:-python3}"
# Resolve $0 so a ~/.local/bin/sesstalk symlink still finds sesstalk.py.
SCRIPT_DIR="$("$PY" -c 'import os, sys; print(os.path.dirname(os.path.realpath(sys.argv[1])))' "$0")"
exec "$PY" -S "$SCRIPT_DIR/sesstalk.py" "$@"
