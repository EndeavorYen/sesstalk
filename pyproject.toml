[project]
name = "sesstalk"
version = "0.5.1"
description = "Same-machine mailbox so Cursor, Claude Code, Codex, and Grok sessions can pass work."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "EndeavorYen" }]
keywords = [
  "ai-agents",
  "cursor",
  "claude-code",
  "codex",
  "session-messaging",
  "mailbox",
]
